<h1>Artists</h1>

<table>
  <tr>
    <th><%= sortable "name", "Artist" %></th>
    <th><%= sortable "created_at", "Created at" %></th>
  </tr>
  <% @artists.each do |artist| %>
    <tr>
      <td><%= artist.name %></td>
      <td><%= artist.created_at.strftime("%d/%m/%Y") %></td>
    </tr>
  <% end %>

</table>
